import React, { useState } from 'react';
// import { TableView } from './TableView';

export function Clients() {

    // const [blockedUsers, setBlockedUsers] = useState([]);


    return (
        <>
            <h1> clients</h1>
            {/* <div>
                <h1>Blocked Users</h1> */}
                {/* Pass blockedUsers as a prop to the TableView component */}
                {/* <TableView blockedUsers={blockedUsers} />
            </div> */}

            {/* <div> */}
                {/* Render blocked users */}
                {/* <h2>Blocked Users</h2>
                <ul>
                    {blockedUsers.map((user, index) => (
                        <li key={index}>
                            Name: {user.fullName}, Pancard: {user.pancard}
                        </li>
                    ))}
                </ul> */}

                {/* Your existing table code */}
            {/* </div> */}
        </>
    );
}